# Core ML
ultralytics>=8.3.0
torch>=2.0.0
torchvision>=0.21.0

# Computer Vision
opencv-python>=4.9.0.80
mediapipe>=0.10.14
Pillow>=10.3.0
albumentations>=1.4.0

# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9
pydantic>=2.7.0
python-jose[cryptography]>=3.3.0

# Data
# NumPy 2.x is required for Python 3.13+ (no 1.x wheels); current
# sentence-transformers / mediapipe work fine against numpy 2.x.
numpy>=2.1.0
pandas>=2.2.2
scikit-learn>=1.4.2
sentence-transformers>=3.4.1
qdrant-client>=1.16.1,<1.17
ollama>=0.6.1

# Experiment Tracking
wandb>=0.16.6

# Speech-to-text
faster-whisper>=1.0.0

# Utilities
tqdm>=4.66.4
PyYAML>=6.0.1
python-dotenv>=1.0.1
requests>=2.31.0

# Visualization
matplotlib>=3.8.4
seaborn>=0.13.2

# NLP
spacy>=3.8.0
# After installing: python -m spacy download en_core_web_sm

# Multi-agent (SPADE + XMPP)
# 4.x uses slixmpp (not aioxmpp), avoiding the lxml~=4.0 pin that has no
# Python 3.13+ wheel and fails to build from source.
spade>=4.0

# Testing
pytest>=8.1.1
pytest-asyncio>=0.23.6
httpx>=0.27.0

# Robotics Bridge
# Capped <3 because SPADE 4.x hard-pins jinja2==3.0.3 (its web dashboard),
# while Flask 3.x requires jinja2>=3.1.2 — irreconcilable. Flask 2.2.x is
# fine for the bridge and accepts jinja2 3.0.3.
Flask>=2.2,<3
paho-mqtt>=2.1.0